<Page x:Class="ProductManager.WpfApp.Views.WarehouseListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Список складів">

    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Page title ──────────────────────────────────────────── -->
        <TextBlock Grid.Row="0"
                   Text="Склади"
                   Style="{StaticResource PageTitleStyle}"/>

        <!-- ── Subtitle / count ────────────────────────────────────── -->
        <TextBlock Grid.Row="1"
                   Margin="0,0,0,16"
                   FontSize="13"
                   Foreground="{StaticResource TextSecondaryBrush}">
            <Run Text="Знайдено складів: "/>
            <Run Text="{Binding Warehouses.Count, Mode=OneWay}"/>
        </TextBlock>

        <!-- ── Loading indicator ───────────────────────────────────── -->
        <TextBlock Grid.Row="2"
                   Text="Завантаження..."
                   FontSize="14"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Visibility="{Binding IsLoading,
                                Converter={StaticResource BoolToVisConverter}}"/>

        <!-- ── Warehouse list ───────────────────────────────────────── -->
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Warehouses}"
                 ItemContainerStyle="{StaticResource ListItemStyle}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Background="Transparent"
                 BorderThickness="0"
                 Visibility="{Binding IsLoading,
                              Converter={StaticResource InverseBoolToVisConverter}}">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Clicking anywhere on the card fires OpenWarehouseCommand -->
                    <Grid>
                        <Grid.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="{Binding DataContext.OpenWarehouseCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListBox}}"
                                          CommandParameter="{Binding}"/>
                        </Grid.InputBindings>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left: icon + name + location -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏭" FontSize="18" Margin="0,0,10,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="28,4,0,0">
                                <TextBlock Text="📍" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding Location}"
                                           Style="{StaticResource LabelStyle}"
                                           FontSize="13"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Right: "Відкрити →" arrow -->
                        <TextBlock Grid.Column="1"
                                   Text="›"
                                   FontSize="28"
                                   Foreground="{StaticResource PrimaryLightBrush}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
